{
  "name": "opk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run",
    "db:migrate:local": "wrangler d1 migrations apply opk-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply opk-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
